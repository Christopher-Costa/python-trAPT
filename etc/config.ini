[general]
interface = ens34
filter = inbound and (arp or icmp or tcp) and not host 10.0.0.10

[app_logging]
log_path = var/log
log_name = trapt.log

[network_logging]
log_path = var/log
log_name = network.log
